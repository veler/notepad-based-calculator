{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "${workspaceFolder}/build.sh Compile --incremental-build true --configuration Debug",
            "windows": {
              "command": "${workspaceFolder}\\build.cmd Compile --incremental-build true --configuration Debug"
            }
        }
    ]
}